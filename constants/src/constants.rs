/*
Constants in Rust

Contstants ensure that values in variables cannot be changed.
The keyword for contants is const.  Constants must be explicitly stated
with the const keyword.

Naming convention for Constants:
All characters in a constant variable should be uppercase by 
standard convention.

*/

fn main() {
    const Y:i16 = 6;
    println!("{}", Y);

}
